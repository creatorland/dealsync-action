name: ds-brand-contacts-backfill-1d-testnet
authority: '0x553b60381dC74493d03b2865C818350f3347Ac7f'
on:
  schedule:
    - cron: '0 13 * * *'
  workflow_dispatch:

concurrency:
  group: brand-contacts-backfill-testnet
  cancel-in-progress: false

jobs:
  brand-contacts-backfill:
    timeout-minutes: 30
    runs-on: ubuntu-latest
    environment: '0xead32ddc4a0d3f3845531916e59a52cdd21c97a41b7ad2806a0cefac57b402bb'
    steps:
      - id: backfill
        name: Run Brand Contacts backfill dispatch
        timeout-minutes: 30
        uses: creatorland/dealsync-action@main
        with:
          command: run-brand-contacts-backfill
          sxt-auth-url: ${{ secrets.SXT_AUTH_URL }}
          sxt-auth-secret: ${{ secrets.SXT_AUTH_SECRET }}
          sxt-api-url: ${{ secrets.SXT_API_URL }}
          sxt-biscuit: ${{ secrets.SXT_DEALSYNC_EMAIL_CORE_BISCUIT }}
          sxt-schema: ${{ secrets.SXT_SCHEMA }}
          email-core-schema: EMAIL_CORE_STAGING
          dealsync-backend-base-url: ${{ secrets.DEALSYNC_BACKEND_BASE_URL }}
          dealsync-v2-shared-secret: ${{ secrets.DEALSYNC_V2_SHARED_SECRET }}
          firestore-service-account-json: ${{ secrets.FIRESTORE_SERVICE_ACCOUNT_JSON }}
          gcp-project-id: ${{ secrets.GCP_PROJECT_ID }}
          backfill-batch-size: '75'
          backfill-concurrency: '5'
          # Uncomment to override action.yml defaults:
          # backfill-attribution-tag: 'brand-contacts-backfill'
          # backfill-dry-run: 'false'
